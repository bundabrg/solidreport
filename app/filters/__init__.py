from .common import format_hours, format_time, pick_color, format_currency

FILTERS = {
    "format_hours": format_hours,
    "format_time": format_time,
    "pick_color": pick_color,
    "format_currency": format_currency,
}
